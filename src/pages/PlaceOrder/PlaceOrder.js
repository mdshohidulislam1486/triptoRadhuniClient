import { Typography } from '@mui/material';
import React from 'react';

const PlaceOrder = () => {
    return (
        <div>
            <Typography>Place your order now </Typography>
        </div>
    );
};

export default PlaceOrder;